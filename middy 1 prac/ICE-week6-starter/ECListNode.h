#ifndef _EC_LIST_NODE_H
#define _EC_LIST_NODE_H

#include <iostream>

// Class for linked list node
class ECListNode
{
public:
  ECListNode() {} //this is a default constructor
  ECListNode *GetNext() const { *pn}//this is
  void SetNext(ECListNode *pn) { ??? }
  int GetValue() const { ??? }
  void SetValue(int v) { ??? }

private:
  // ???
};

#endif



//ok lets work on filling this out

//ok so we have a class with multiple parts
//public and private
//ok so in H files these should be constructors?

//cannot understand this 
//try to do some other practice and come back